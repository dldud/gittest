/**
 * Created by KICT-05 on 2016-07-19.
 */
const electron = require('electron');
const {app} = electron;
const {BrowerWindow} = electron;
let win;
function createWindow() {
    win = new BrowerWindow({width:800,height:600});

    win.on('closed',()=>{
        win =null;
    });
}

app.on('ready',createWindow);
app.on('activate',()=> {
    if(win === null)
})